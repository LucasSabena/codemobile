export default function App() {
  return (
    <main className="page">
      <section className="card">
        <p className="tag">Sample React</p>
        <h1>CodeMobile React Sample</h1>
        <p>
          Este sample ya es React + Vite. Si hay runtime Node/NPM disponible, ejecuta
          <code> npm install </code> y luego <code> npm run dev </code>.
        </p>
      </section>
    </main>
  )
}
